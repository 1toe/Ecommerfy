import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app334.shopper',
  appName: 'Shopper Cart',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
